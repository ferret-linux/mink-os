# Create the i2c group (if missing) so users can be granted access to
# /dev/i2c-* devices without root — e.g. for OpenRGB lighting control or
# ddcutil monitor DDC/CI control. Actual device permissions are set via
# a udev rule (SUBSYSTEM=="i2c-dev", GROUP="i2c", MODE="0660").
g i2c - - -