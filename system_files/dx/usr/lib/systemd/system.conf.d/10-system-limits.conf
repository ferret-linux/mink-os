[Manager]
# Raise default per-service file descriptor limits — soft 65536 (covers most
# processes without extra tuning), hard ceiling 2097152 to match fs.file-max
# sysctl, letting FD-hungry services (games, DBs, dev tools) raise their own
# limit as needed without a per-unit override
DefaultLimitNOFILE=65536:2097152