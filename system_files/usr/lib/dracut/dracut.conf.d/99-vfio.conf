# Include VFIO modules in the initramfs so they're available early enough
# to claim the passthrough GPU before amdgpu/nvidia binds to it
# (requires vfio-pci.ids= or equivalent to specify which device to bind)
add_drivers+=" vfio vfio_iommu_type1 vfio_pci "