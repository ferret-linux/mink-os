# Blacklist hardware watchdog timers — Intel TCO (iTCO_wdt) and AMD SP5100
# chipset (sp5100_tco). These can cause spurious reboots / conflict with
# [systemd-watchdog | VM passthrough] if left enabled.
blacklist iTCO_wdt
blacklist sp5100_tco